<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.hiep.duan1_quanlyhoctap">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ManHinhChao"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Manhinhdangnhap" />
        <activity android:name=".MainActivity" />
        <activity android:name=".dangky" />
        <activity android:name=".introduce" />
        <activity android:name=".QuanLyLop" />
        <activity android:name=".Quanly_monhoc" />
        <activity android:name=".Quanlydiem" />
        <activity android:name=".fragment_lichnghi" />
    </application>

</manifest>